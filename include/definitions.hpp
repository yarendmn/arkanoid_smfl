#ifndef DEFINITIONS_HPP
#define DEFINITIONS_HPP

// screen settings
const int WINDOW_WIDTH = 800;
const int WINDOW_HEIGHT = 600;

// speed settings
const float PADDLE_SPEED = 8.0f; // pixels per second
const float BALL_SPEED_X = 4.0f; // pixels per second
const float BALL_SPEED_Y = -4.0f; // pixels per second

// object sizes
const float PADDLE_WIDTH = 100.0f;
const float PADDLE_HEIGHT = 20.0f;
const float BALL_RADIUS = 10.0f;
const float BRICK_WIDTH = 80.0f;
const float BRICK_HEIGHT = 25.0f;
const float BRICK_OFFSET = 10.0f; // space between bricks


#endif // DEFINITIONS_HPP